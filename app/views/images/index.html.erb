<h1>Listing images</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @images.each do |image| %>
  <tr>
    <td><%= link_to 'Show', display_image_path(image) %></td>
    <td><%= link_to edit_display_image_url(image) %></td>
    <td><%= link_to 'Destroy', display_image_path(image), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Image', new_display_image_path %>
